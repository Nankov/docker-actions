name: Build Docker image workflow

on:
  workflow_call:
    inputs:
      image-name:
        required: true
        type: string

jobs:
  build-image:
    runs-on: ubuntu-latest
    steps:
      - name: Building image
        run: docker build -t ${{inputs.image-name}}
